# wrangler local state (contains account metadata)
.wrangler/
node_modules/
# local-only wrangler secrets (throwaway VAPID pair for wrangler dev)
worker/.dev.vars
# desktop-app scratch + personal files that live beside the repo but are not part of it
.claude/
files/
worker/dev.log
christian-app-game-plan.html
prompt-generator.html
